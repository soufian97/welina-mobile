import { defineMessage } from '../I18n/defineMessage';

const scope = 'app.components.RenderNoData';

export const translation = defineMessage({
  noResult: {
    id: `${scope}.noResult`,
    defaultMessage: 'There is no result',
  },
});
